# 核心依赖
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
Pillow>=9.0.0
tqdm>=4.64.0
pyyaml>=6.0
scipy>=1.9.0
matplotlib>=3.5.0

# Hugging Face Transformers (用于加载预训练模型)
transformers>=4.35.0

# 深度估计 - Depth Anything V2
# 通过transformers加载，无需额外安装

# 语义分割 - Mask2Former, SegFormer, OneFormer
# 通过transformers加载，无需额外安装

# SAM评测需要sklearn
scikit-learn>=1.0.0

# 图像质量指标
scikit-image>=0.19.0   # SSIM
lpips>=0.1.4           # LPIPS感知相似度
clean-fid>=0.1.35      # FID计算

# FVD (Fréchet Video Distance)
cd-fvd>=0.2.0          # FVD计算 (I3D Kinetics-400, CVPR 2024标准)
einops>=0.6.0          # cd-fvd依赖

# 可选：timm (某些模型backbone需要)
timm>=0.9.0

# 可选：accelerate (加速推理)
accelerate>=0.25.0
